apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: solo-leveling-web
spec:
  template:
    spec:
      containers:
        - image: REGION-docker.pkg.dev/PROJECT_ID/solo-leveling/web:latest
          ports:
            - containerPort: 80
